<html>
    <head>
        <meta charset="UTF-8">
        <title>Title of the PDF</title>
    </head>
        {% block body %}
            <h4>{{ title }}</h4>
            <p>Lorem Ipsum</p>
            <p>{{event.dateRendezVous|date("H:i")}}</p>
            <p>{{event.idClient.getClient()}}</p>
            <p>{{event.idCollaborateur.getCollaborateur()}}</p>
            <p>{{event.idPrestation.getPrestation()}}</p>
        {% endblock %}
</html>