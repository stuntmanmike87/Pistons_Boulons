{% extends 'base.html.twig' %}

{% block title %}Dernières connexions collaborateurs
{% endblock %}

{% block body %}
	<div id="main-wrapper">
		<div class="container">
			
			<h1 class="mt-5">Liste des dernières connexions des collaborateurs :</h1>

			<table class="table_responsive_mobile mt-5">
				<thead>
					<tr>
						<th>Nom</th>
						<th>Prénom</th>
						<th>Date</th>
								<th>Heure</th>
					</tr>
				</thead>
				<tbody>
					{% for collaborateur in collaborateurs %}
						<tr>
							<td data-label="Nom">{{ collaborateur.nom }}</td>
							<td data-label="Prénom">{{ collaborateur.prenom }}</td>
							<td>{{ collaborateur.dateHeureDerniereConnexion ? collaborateur.dateHeureDerniereConnexion|date('d-m-Y', "Europe/Paris") : '' }}</td>
							<td>{{ collaborateur.dateHeureDerniereConnexion ? collaborateur.dateHeureDerniereConnexion|date('H:i:s') : '' }}</td>

						</tr>
					{% else %}
						<tr>
							<td colspan="8" class="text-center maintenance mt-3">Aucun collaborateur</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>


		</div>
	</div>
{% endblock %}
