<form method="post" action="{{ path('collaborateur_delete', {'id': collaborateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collaborateur.id) }}">
    <button class="btn">Delete</button>
</form>
