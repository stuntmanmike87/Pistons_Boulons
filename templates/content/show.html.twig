{% extends 'base.html.twig' %}

{% block title %}Contenu
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<h1>Contenu du site</h1>


		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ content.id }}</td>
				</tr>
				<tr>
					<th>Texte</th>
					<td>{{ content.text }}</td>
				</tr>
				<tr>
					<th>Position</th>
					<td>{{ content.position }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('content_index') }}"><button class="mt-2">Retour</button></a>

        <a href="{{ path('content_edit', {'id': content.id}) }}"><button class="mt-2">Éditer</button></a>


		{{ include('content/_delete_form.html.twig') }}

	</div>
{% endblock %}
