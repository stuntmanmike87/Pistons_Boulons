<form class="" method="post" action="{{ path('prestation_delete', {'id': prestation.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette prestation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prestation.id) }}">
    <button class="">Supprimer</button>
</form>
